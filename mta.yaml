_schema-version: "3.1"
ID: FioriTest
description: A simple CAP project.
version: 1.0.0
modules:
- name: FioriTest-srv
  type: nodejs
  path: gen/srv
  requires:
  - name: FioriTest-db
  - name: FioriTest-auth
  provides:
  - name: srv-api
    properties:
      srv-url: ${default-url}
  parameters:
    buildpack: nodejs_buildpack
  build-parameters:
    builder: npm-ci
- name: FioriTest-db-deployer
  type: hdb
  path: gen/db
  requires:
  - name: FioriTest-db
  parameters:
    buildpack: nodejs_buildpack
- name: FioriTest
  type: approuter.nodejs
  path: app/
  requires:
  - name: srv-api
    group: destinations
    properties:
      forwardAuthToken: true
      name: srv-api
      url: ~{srv-url}
  - name: FioriTest-auth
  - name: FioriTest-destination-service
    group: destinations
    properties:
      forwardAuthToken: false
      name: ui5
      url: https://ui5.sap.com
  parameters:
    disk-quota: 2560M
    keep-existing-routes: true
    memory: 2560M
- name: FioriTest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: FioriTest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - sapfiorisapfiori.zip
      name: sapfiorisapfiori
      target-path: resources/
- name: sapfiorisapfiori
  type: html5
  path: app/sapfiori
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: FioriTest-db
  type: com.sap.xs.hdi-container
  parameters:
    service: hana
    service-plan: hdi-shared
- name: FioriTest-auth
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      tenant-mode: dedicated
      xsappname: FioriTest-${org}-${space}
    path: ./xs-security.json
    service: xsuaa
    service-plan: application
- name: FioriTest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: FioriTest-html5-srv
    service-plan: app-host
- name: FioriTest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: false
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          - Authentication: NoAuthentication
            HTML5.DynamicDestination: true
            HTML5.ForwardAuthToken: true
            Name: FioriTest-srv-api
            ProxyType: Internet
            Type: HTTP
            URL: ~{srv-api/srv-url}
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: FioriTest-destination-service
    service-plan: lite
  requires:
  - name: srv-api
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
build-parameters:
  before-all:
  - builder: custom
    commands:
    - npx -p @sap/cds-dk cds build --production
